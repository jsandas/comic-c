/*
 * runtime_stubs.c - Runtime support stubs
 * 
 * These are placeholders for Open Watcom runtime symbols
 * that are generated but not needed for our 16-bit DOS executable.
 */

/* _big_code_ symbol - used by Open Watcom for large model code */
#pragma aux _big_code_ "*"
int _big_code_ = 1;
